import {
  actions as tActions,
  getters as tGetters,
  mutations as tMutations,
} from "./treemap/module";

export const treemapGetters = tGetters;
export const treemapActions = tActions;
export const treemapMutations = tMutations;
